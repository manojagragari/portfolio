[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "portfolio"
version = "1.0.0"
description = "Full-stack portfolio application with Django backend and Next.js frontend"
requires-python = ">=3.11"

[tool.vercel]
framework = "django"
